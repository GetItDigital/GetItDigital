\begin{tikzpicture}
    \ctikzset{tripoles/en amp/input height=0.45}
    \draw (0,0) node[en amp, anchor=center] (opamp) {};
    \draw (opamp.up) -- ++(0,0.5) node[right] {$+$}; % Text leicht rechts
    \draw (opamp.down) -- ++(0,-0.5) node[right] {$-$}; % Text leicht rechts

    \draw (opamp.out) to[short, -*] (4,0)to[R,l=$R_\mathrm{Last}$] (4,-1.5) to[short,-*] (-1.2,-1.5)node[ground]{} -- (-1.2,-0.5) to (opamp.+);

    %(0,0) node[en amp, anchor=center] (opamp) {};

    \draw (opamp.-) to[short, -*] (-1.2,1) -- (-1.2,2) -- (1,2) to[R,l=$R_\mathrm{R}$](4,2)--(4,0);
    \draw (-3.8,1) to[R,l=$R_\mathrm{E}$] (-1.2,1);

    \draw (opamp.up) -- (0,4) to[short, -*] (-4,4) -- (-6,4) to[V,name=V1] (-6,2)node[ground]{};

    \draw (opamp.down) -- (0,-3) to[short,-*](-4,-3) -- (-6,-3) to[V,name=V2] (-6,-5)node[ground]{};
    \draw (-4,-3) to[R,l=$R_\mathrm{2}$] (-4,0) to[potentiometer, l=$P_\mathrm{1}$] (-4,2) to [R,l=$R_\mathrm{1}$](-4,4);

    \draw (-1.2,-1.5) to[short, -o] (-3.1,-1.5);

    \draw (-3.1,1) to[open,v,name=ue](-3.1,-1.5);
    \draw (3,0) to[open,v,name=ua](3,-1.5);

    \varrmore{ua}{$U_\mathrm{A}$};
    \varrmore{ue}{$U_\mathrm{E}$};
    \varrmore{V1}{$U_\mathrm{1}$};
    \varrmore{V2}{$U_\mathrm{2}$};
\end{tikzpicture}
   